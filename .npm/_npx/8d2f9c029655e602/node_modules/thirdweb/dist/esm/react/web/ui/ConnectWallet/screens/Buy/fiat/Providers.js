import { jsx as _jsx } from "react/jsx-runtime";
import { Container } from "../../../../components/basic.js";
import { Button } from "../../../../components/buttons.js";
import { Link } from "../../../../components/text.js";
import { getProviderLabel } from "../utils.js";
/**
 * @internal
 */
export function Providers(props) {
    return (_jsx(Container, { expand: true, flex: "column", gap: "sm", style: {
            alignItems: "flex-start",
        }, children: props.supportedProviders.map((provider) => (_jsx(Container, { expand: true, flex: "row", style: {
                justifyContent: "space-between",
            }, children: _jsx(Button, { fullWidth: true, onClick: () => props.onSelect(provider), variant: "link", children: _jsx(Link, { color: props.preferredProvider === provider
                        ? "primaryText"
                        : "secondaryText", hoverColor: "primaryText", size: "sm", children: getProviderLabel(provider) }) }) }, provider))) }));
}
//# sourceMappingURL=Providers.js.map