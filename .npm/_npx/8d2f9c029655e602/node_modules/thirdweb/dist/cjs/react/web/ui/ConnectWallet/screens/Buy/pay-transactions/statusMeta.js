"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getBuyWithCryptoStatusMeta = getBuyWithCryptoStatusMeta;
function getBuyWithCryptoStatusMeta(cryptoStatus) {
    if (cryptoStatus.status === "NOT_FOUND") {
        return {
            color: "secondaryText",
            status: "Unknown",
        };
    }
    const subStatus = cryptoStatus.subStatus;
    const status = cryptoStatus.status;
    if (subStatus === "WAITING_BRIDGE") {
        return {
            color: "accentText",
            loading: true,
            status: "Bridging",
        };
    }
    if (subStatus === "PARTIAL_SUCCESS") {
        return {
            color: "secondaryText",
            status: "Incomplete",
        };
    }
    if (status === "PENDING") {
        return {
            color: "accentText",
            loading: true,
            status: "Pending",
        };
    }
    if (status === "FAILED") {
        return {
            color: "danger",
            status: "Failed",
        };
    }
    if (status === "COMPLETED") {
        return {
            color: "success",
            status: "Completed",
        };
    }
    return {
        color: "secondaryText",
        status: "Unknown",
    };
}
//# sourceMappingURL=statusMeta.js.map